进度&想要做的东西
大纲
1.资料处理（done）
2.挑选特征（done）（有没有做onehot结果差不多但是做了onehot后面分析资料会指数增长使用先不做，特征的部分先用决策树给出的较高权重特征）
3.一堆特征做67种车祸类别分类（最后选择全连接&决策树-》效果比较快也比较好）
4.依照分类结果去使用shap等模型解释看是哪一种特征变化导致车祸上升（卡关不会用shap）

正要做的东西
简化特征-》方便看决策树的图表以及shap的分类结果
将特征再切除测试集（目前全部用来训练和验证）


可以做的东西
处理缺失值-代理分支（surrogate branch）用来在模型训练过程中处理缺失特征（missing features）。通过设计一个代理分支来预测这些缺失特征，模型可以在训练阶段利用预测的特征来增强主任务的学习效果

已经做的东西
SVM-久到睡着
RNN-数据本身看不出时序影响，撇开来说以准确率来说还是没有比较好
CNN-尝试地区性会不会对分析结果有影响(不确定是不是一维资料展开方法的问题)->看起来效果没全连接&决策树好
WGAN-非图像数据不太会做生，成出来的loss太高
